# ── PROTECTED: Infrastructure & Secrets ──
# Docker orchestration (no way to spin up services without this)
docker-compose.yml
docker-compose.*.yml

# Nginx gateway config (no routing without this)
nginx.conf
nginx/

# All service .env files (DB creds, APP_KEY, JWT secrets)
.env
*/.env
**/.env
*.env

# Database schema (migrations reveal full table structure)
*/database/migrations/

# API routes (reveals all endpoints and their logic)
*/routes/api.php
*/routes/web.php
*/routes/channels.php

# ── Laravel per-service ignores ──
*/vendor/
*/node_modules/
*/storage/logs/
*/storage/framework/cache/
*/storage/framework/sessions/
*/storage/framework/views/
*/bootstrap/cache/
*/.php-cs-fixer.cache
*/public/storage

# IDE
.idea/
.DS_Store
*.iml

# Keep storage structure
!*/storage/logs/.gitkeep
!*/storage/framework/.gitkeep
